{% extends 'base.html.twig' %}

{% block title %}User Form {% endblock %}

{% block body %}

 <div class="container">
 <div class="row">
  <div class="m-4">
   <h1>User Form</h1>
 {{ form_start (monForm) }}
 {{ form_row(monForm.first_name) }}
 {{ form_row(monForm.last_name) }}
 {{ form_row(monForm.email) }}
   <button type="submit" class="btn btn-primary">Valider</button>
  </div>
 </div>
 </div>

 {{ form_end(monForm) }}
{% endblock %}
